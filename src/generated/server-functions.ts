// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated Client Functions
 *
 * Fetch-based CRUD operations that call the Hono REST API.
 * No server-side imports — safe for browser bundles.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  CourseType,
  CourseCreateInput,
  CourseUpdateInput,
  ModuleType,
  ModuleCreateInput,
  ModuleUpdateInput,
  LessonType,
  LessonCreateInput,
  LessonUpdateInput,
  EnrollmentType,
  EnrollmentCreateInput,
  EnrollmentUpdateInput,
  ProgressType,
  ProgressCreateInput,
  ProgressUpdateInput,
  LiveSessionType,
  LiveSessionCreateInput,
  LiveSessionUpdateInput,
  QuizResultType,
  QuizResultCreateInput,
  QuizResultUpdateInput,
  PasswordResetTokenType,
  PasswordResetTokenCreateInput,
  PasswordResetTokenUpdateInput,
  JournalEntryType,
  JournalEntryCreateInput,
  JournalEntryUpdateInput,
} from './types'

/** Get the API base URL */
function getApiBase(): string {
  if (typeof window !== 'undefined') {
    return window.location.origin
  }
  return process.env.API_URL || 'http://localhost:3001'
}

// ============================================================================
// User Client Functions
// ============================================================================

/**
 * List all User records
 */
export async function getUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list User')
  }
  const json = await response.json()
  return (json.items || []) as UserType[]
}

/**
 * Get a single User by ID
 */
export async function getUserById(args: { data: { id: string; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'User not found')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Create a new User
 */
export async function createUser(args: { data: { input: UserCreateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Update an existing User
 */
export async function updateUser(args: { data: { id: string; input: UserUpdateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Delete a User
 */
export async function deleteUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete User')
  }
  return { success: true }
}

// ============================================================================
// Course Client Functions
// ============================================================================

/**
 * List all Course records
 */
export async function getCourseList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<CourseType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/courses${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Course')
  }
  const json = await response.json()
  return (json.items || []) as CourseType[]
}

/**
 * Get a single Course by ID
 */
export async function getCourseById(args: { data: { id: string; userId?: string } }): Promise<CourseType> {
  const url = `${getApiBase()}/api/courses/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Course not found')
  }
  const json = await response.json()
  return json.data as CourseType
}

/**
 * Create a new Course
 */
export async function createCourse(args: { data: { input: CourseCreateInput; userId?: string } }): Promise<CourseType> {
  const url = `${getApiBase()}/api/courses`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Course')
  }
  const json = await response.json()
  return json.data as CourseType
}

/**
 * Update an existing Course
 */
export async function updateCourse(args: { data: { id: string; input: CourseUpdateInput; userId?: string } }): Promise<CourseType> {
  const url = `${getApiBase()}/api/courses/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Course')
  }
  const json = await response.json()
  return json.data as CourseType
}

/**
 * Delete a Course
 */
export async function deleteCourse(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/courses/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Course')
  }
  return { success: true }
}

// ============================================================================
// Module Client Functions
// ============================================================================

/**
 * List all Module records
 */
export async function getModuleList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ModuleType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/modules${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Module')
  }
  const json = await response.json()
  return (json.items || []) as ModuleType[]
}

/**
 * Get a single Module by ID
 */
export async function getModuleById(args: { data: { id: string; userId?: string } }): Promise<ModuleType> {
  const url = `${getApiBase()}/api/modules/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Module not found')
  }
  const json = await response.json()
  return json.data as ModuleType
}

/**
 * Create a new Module
 */
export async function createModule(args: { data: { input: ModuleCreateInput; userId?: string } }): Promise<ModuleType> {
  const url = `${getApiBase()}/api/modules`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Module')
  }
  const json = await response.json()
  return json.data as ModuleType
}

/**
 * Update an existing Module
 */
export async function updateModule(args: { data: { id: string; input: ModuleUpdateInput; userId?: string } }): Promise<ModuleType> {
  const url = `${getApiBase()}/api/modules/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Module')
  }
  const json = await response.json()
  return json.data as ModuleType
}

/**
 * Delete a Module
 */
export async function deleteModule(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/modules/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Module')
  }
  return { success: true }
}

// ============================================================================
// Lesson Client Functions
// ============================================================================

/**
 * List all Lesson records
 */
export async function getLessonList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<LessonType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/lessons${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Lesson')
  }
  const json = await response.json()
  return (json.items || []) as LessonType[]
}

/**
 * Get a single Lesson by ID
 */
export async function getLessonById(args: { data: { id: string; userId?: string } }): Promise<LessonType> {
  const url = `${getApiBase()}/api/lessons/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Lesson not found')
  }
  const json = await response.json()
  return json.data as LessonType
}

/**
 * Create a new Lesson
 */
export async function createLesson(args: { data: { input: LessonCreateInput; userId?: string } }): Promise<LessonType> {
  const url = `${getApiBase()}/api/lessons`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Lesson')
  }
  const json = await response.json()
  return json.data as LessonType
}

/**
 * Update an existing Lesson
 */
export async function updateLesson(args: { data: { id: string; input: LessonUpdateInput; userId?: string } }): Promise<LessonType> {
  const url = `${getApiBase()}/api/lessons/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Lesson')
  }
  const json = await response.json()
  return json.data as LessonType
}

/**
 * Delete a Lesson
 */
export async function deleteLesson(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/lessons/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Lesson')
  }
  return { success: true }
}

// ============================================================================
// Enrollment Client Functions
// ============================================================================

/**
 * List all Enrollment records
 */
export async function getEnrollmentList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<EnrollmentType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/enrollments${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Enrollment')
  }
  const json = await response.json()
  return (json.items || []) as EnrollmentType[]
}

/**
 * Get a single Enrollment by ID
 */
export async function getEnrollmentById(args: { data: { id: string; userId?: string } }): Promise<EnrollmentType> {
  const url = `${getApiBase()}/api/enrollments/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Enrollment not found')
  }
  const json = await response.json()
  return json.data as EnrollmentType
}

/**
 * Create a new Enrollment
 */
export async function createEnrollment(args: { data: { input: EnrollmentCreateInput; userId?: string } }): Promise<EnrollmentType> {
  const url = `${getApiBase()}/api/enrollments`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Enrollment')
  }
  const json = await response.json()
  return json.data as EnrollmentType
}

/**
 * Update an existing Enrollment
 */
export async function updateEnrollment(args: { data: { id: string; input: EnrollmentUpdateInput; userId?: string } }): Promise<EnrollmentType> {
  const url = `${getApiBase()}/api/enrollments/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Enrollment')
  }
  const json = await response.json()
  return json.data as EnrollmentType
}

/**
 * Delete a Enrollment
 */
export async function deleteEnrollment(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/enrollments/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Enrollment')
  }
  return { success: true }
}

// ============================================================================
// Progress Client Functions
// ============================================================================

/**
 * List all Progress records
 */
export async function getProgressList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ProgressType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/progresses${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Progress')
  }
  const json = await response.json()
  return (json.items || []) as ProgressType[]
}

/**
 * Get a single Progress by ID
 */
export async function getProgressById(args: { data: { id: string; userId?: string } }): Promise<ProgressType> {
  const url = `${getApiBase()}/api/progresses/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Progress not found')
  }
  const json = await response.json()
  return json.data as ProgressType
}

/**
 * Create a new Progress
 */
export async function createProgress(args: { data: { input: ProgressCreateInput; userId?: string } }): Promise<ProgressType> {
  const url = `${getApiBase()}/api/progresses`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Progress')
  }
  const json = await response.json()
  return json.data as ProgressType
}

/**
 * Update an existing Progress
 */
export async function updateProgress(args: { data: { id: string; input: ProgressUpdateInput; userId?: string } }): Promise<ProgressType> {
  const url = `${getApiBase()}/api/progresses/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Progress')
  }
  const json = await response.json()
  return json.data as ProgressType
}

/**
 * Delete a Progress
 */
export async function deleteProgress(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/progresses/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Progress')
  }
  return { success: true }
}

// ============================================================================
// LiveSession Client Functions
// ============================================================================

/**
 * List all LiveSession records
 */
export async function getLiveSessionList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<LiveSessionType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/live-sessions${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list LiveSession')
  }
  const json = await response.json()
  return (json.items || []) as LiveSessionType[]
}

/**
 * Get a single LiveSession by ID
 */
export async function getLiveSessionById(args: { data: { id: string; userId?: string } }): Promise<LiveSessionType> {
  const url = `${getApiBase()}/api/live-sessions/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'LiveSession not found')
  }
  const json = await response.json()
  return json.data as LiveSessionType
}

/**
 * Create a new LiveSession
 */
export async function createLiveSession(args: { data: { input: LiveSessionCreateInput; userId?: string } }): Promise<LiveSessionType> {
  const url = `${getApiBase()}/api/live-sessions`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create LiveSession')
  }
  const json = await response.json()
  return json.data as LiveSessionType
}

/**
 * Update an existing LiveSession
 */
export async function updateLiveSession(args: { data: { id: string; input: LiveSessionUpdateInput; userId?: string } }): Promise<LiveSessionType> {
  const url = `${getApiBase()}/api/live-sessions/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update LiveSession')
  }
  const json = await response.json()
  return json.data as LiveSessionType
}

/**
 * Delete a LiveSession
 */
export async function deleteLiveSession(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/live-sessions/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete LiveSession')
  }
  return { success: true }
}

// ============================================================================
// QuizResult Client Functions
// ============================================================================

/**
 * List all QuizResult records
 */
export async function getQuizResultList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<QuizResultType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/quiz-results${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list QuizResult')
  }
  const json = await response.json()
  return (json.items || []) as QuizResultType[]
}

/**
 * Get a single QuizResult by ID
 */
export async function getQuizResultById(args: { data: { id: string; userId?: string } }): Promise<QuizResultType> {
  const url = `${getApiBase()}/api/quiz-results/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'QuizResult not found')
  }
  const json = await response.json()
  return json.data as QuizResultType
}

/**
 * Create a new QuizResult
 */
export async function createQuizResult(args: { data: { input: QuizResultCreateInput; userId?: string } }): Promise<QuizResultType> {
  const url = `${getApiBase()}/api/quiz-results`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create QuizResult')
  }
  const json = await response.json()
  return json.data as QuizResultType
}

/**
 * Update an existing QuizResult
 */
export async function updateQuizResult(args: { data: { id: string; input: QuizResultUpdateInput; userId?: string } }): Promise<QuizResultType> {
  const url = `${getApiBase()}/api/quiz-results/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update QuizResult')
  }
  const json = await response.json()
  return json.data as QuizResultType
}

/**
 * Delete a QuizResult
 */
export async function deleteQuizResult(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/quiz-results/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete QuizResult')
  }
  return { success: true }
}

// ============================================================================
// PasswordResetToken Client Functions
// ============================================================================

/**
 * List all PasswordResetToken records
 */
export async function getPasswordResetTokenList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<PasswordResetTokenType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/password-reset-tokens${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list PasswordResetToken')
  }
  const json = await response.json()
  return (json.items || []) as PasswordResetTokenType[]
}

/**
 * Get a single PasswordResetToken by ID
 */
export async function getPasswordResetTokenById(args: { data: { id: string; userId?: string } }): Promise<PasswordResetTokenType> {
  const url = `${getApiBase()}/api/password-reset-tokens/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'PasswordResetToken not found')
  }
  const json = await response.json()
  return json.data as PasswordResetTokenType
}

/**
 * Create a new PasswordResetToken
 */
export async function createPasswordResetToken(args: { data: { input: PasswordResetTokenCreateInput; userId?: string } }): Promise<PasswordResetTokenType> {
  const url = `${getApiBase()}/api/password-reset-tokens`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create PasswordResetToken')
  }
  const json = await response.json()
  return json.data as PasswordResetTokenType
}

/**
 * Update an existing PasswordResetToken
 */
export async function updatePasswordResetToken(args: { data: { id: string; input: PasswordResetTokenUpdateInput; userId?: string } }): Promise<PasswordResetTokenType> {
  const url = `${getApiBase()}/api/password-reset-tokens/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update PasswordResetToken')
  }
  const json = await response.json()
  return json.data as PasswordResetTokenType
}

/**
 * Delete a PasswordResetToken
 */
export async function deletePasswordResetToken(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/password-reset-tokens/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete PasswordResetToken')
  }
  return { success: true }
}

// ============================================================================
// JournalEntry Client Functions
// ============================================================================

/**
 * List all JournalEntry records
 */
export async function getJournalEntryList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<JournalEntryType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/journal-entries${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list JournalEntry')
  }
  const json = await response.json()
  return (json.items || []) as JournalEntryType[]
}

/**
 * Get a single JournalEntry by ID
 */
export async function getJournalEntryById(args: { data: { id: string; userId?: string } }): Promise<JournalEntryType> {
  const url = `${getApiBase()}/api/journal-entries/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'JournalEntry not found')
  }
  const json = await response.json()
  return json.data as JournalEntryType
}

/**
 * Create a new JournalEntry
 */
export async function createJournalEntry(args: { data: { input: JournalEntryCreateInput; userId?: string } }): Promise<JournalEntryType> {
  const url = `${getApiBase()}/api/journal-entries`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create JournalEntry')
  }
  const json = await response.json()
  return json.data as JournalEntryType
}

/**
 * Update an existing JournalEntry
 */
export async function updateJournalEntry(args: { data: { id: string; input: JournalEntryUpdateInput; userId?: string } }): Promise<JournalEntryType> {
  const url = `${getApiBase()}/api/journal-entries/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update JournalEntry')
  }
  const json = await response.json()
  return json.data as JournalEntryType
}

/**
 * Delete a JournalEntry
 */
export async function deleteJournalEntry(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/journal-entries/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete JournalEntry')
  }
  return { success: true }
}
